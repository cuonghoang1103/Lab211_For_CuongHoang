package constants;

/**
 * Every message and label the program shows on screen.
 *
 * @author HE176322
 */
public final class Message {

    // Title above the sequence (the brief's screen); %d is the count.
    public static final String TITLE = "The %d sequence fibonacci:";

    // Private constructor: this class only holds constants.
    private Message() {
    }
}
